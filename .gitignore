# Runtime volume created by `docker run -v $PWD/config:/config`
config/

# DIND data + gocryptfs host mounts
docker-data/
host-unencrypted/
host-encrypted/

# Test evidence images / large downloads
*.dd
*.e01
*.E01
autopsy-*.zip
sleuthkit-*.tar.gz
openjfx*.zip

.DS_Store

# WireGuard/VPN configs (secrets) for the gluetun sidecar
vpn-configs/

# Real compose files hold runtime config (env, VPN choice) — track the .example templates instead
docker-compose.yaml
